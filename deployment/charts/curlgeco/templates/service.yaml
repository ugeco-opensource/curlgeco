apiVersion: v1
kind: Service
metadata:
  name: {{ include "curlgeco.fullname" . }}
  labels:
    {{- include "curlgeco.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "curlgeco.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
